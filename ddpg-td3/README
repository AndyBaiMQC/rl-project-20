Recommend to run on GPU:

To train on Colab/Notebook:
!xvfb-run -a -s "-screen 0 1400x900x24 +extension RANDR" -- python car_racing.py [-algo, 'TD3' or 'DDPG']

To train on local machine with GUI:
python car_racing.py [-algo, 'TD3' or 'DDPG']
